# challenge name(s); defaults to all compatible challenges
challenge: ["mooc-collision-check-test"]

# conventions + format
protocol: "protocol_collision_checking-1"

# any user label - displayed in "challenges list"
user-label: "collision-check-exercise"

# any JSON payload
user-payload: {}

